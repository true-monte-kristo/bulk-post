[project]
name = "bulk-post"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
bulk-post = "bulk_post:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
